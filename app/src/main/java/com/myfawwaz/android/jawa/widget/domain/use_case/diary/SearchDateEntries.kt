package com.myfawwaz.android.jawa.widget.domain.use_case.diary

/*
class SearchDateEntries @Inject constructor(
    private val repository: DiaryRepository
) {
    suspend operator fun invoke(dateSearch: Long) = repository.searchDate(dateSearch)
}

 */